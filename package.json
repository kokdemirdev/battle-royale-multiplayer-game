{
  "name": "battle-royale-multiplayer-game",
  "version": "1.0.0",
  "scripts": {
    "dev:server": "yarn --cwd server start:dev",
    "dev:client": "wait-on http://127.0.0.1:8080 && yarn --cwd client dev",
    "dev": "cross-env FORCE_COLOR=1 npm-run-all -l -p dev:*"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "wait-on": "^6.0.1",
    "cross-env": "^7.0.3"
  }
}
